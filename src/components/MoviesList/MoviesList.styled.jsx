import styled from 'styled-components';

export const Layout = styled.ul`
  list-style: circle;
  padding: 0;
  margin: 0;
  margin-left: 20px;
`;

export const Text = styled.p`
  font-size: 16px;
`;
